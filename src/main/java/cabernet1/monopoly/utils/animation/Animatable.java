package cabernet1.monopoly.utils.animation;

/**
 *  Purpose:	The Animatable interface allows objects to register
 *    		with the animator.  It defines one method, a draw
 *		method, that takes a single parameter, a Graphics
 *		object.
 */
public interface Animatable {
    public void animate();
}
